type: "object"
properties:
  isbn:
    type: "string"
  title:
    type: "string"
  description:
    type: "string"
  category:
    type: "array"
    description: "An array of category strings"
    items:
      type: "string"
  onLoan:
    type: "boolean"
  authors:
    type: "array"
    description: "An array of authorId strings"
    items:
      type: "string"
  coverpage: # need to have file upload interface in referencing yamls
    type: "string"
    format: binary
xml:
  name: "Update Book"